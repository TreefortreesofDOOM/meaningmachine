import { SeamEvent } from 'seamapi-types';

type SeamWebhookEvent = Distribute<SeamEvent['event_type']>;
declare class SeamWebhook {
    #private;
    constructor(secret: string);
    verify(payload: string, headers: Record<string, string>): SeamWebhookEvent;
}
type Distribute<EventType> = EventType extends SeamEvent['event_type'] ? {
    event_type: EventType;
} & Extract<SeamEvent, {
    event_type: EventType;
}>['payload'] : never;

export { SeamWebhook, type SeamWebhookEvent };
